# ── Dependencies ─────────────────────────────────────────────
node_modules/
.pnp
.pnp.js

# ── Build output ──────────────────────────────────────────────
dist/
build/
client/dist/
server/dist/

# ── Database (local data — never commit) ─────────────────────
*.db
*.db-shm
*.db-wal

# ── Environment & secrets ─────────────────────────────────────
.env
.env.local
.env.*.local

# ── OS / editor junk ─────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo
.idea/
.vscode/

# ── Logs ─────────────────────────────────────────────────────
*.log
npm-debug.log*

# ── TypeScript cache ─────────────────────────────────────────
*.tsbuildinfo

# ── Vite / PWA generated icons (reproducible, not source) ────
client/public/icons/


server/public/audio/

# ── Merged in from the old 'The Hifz App' folder ──────────────────
# Live archive.org S3 credentials — must never reach the public repo.
Key.txt
# 2.47 GB of recitations. Also present under server/public/audio, which the
# server serves locally; both are far too large for git.
Page Audio Recordings/

# Machine-specific Claude settings and a vendored tool bundle - not project code.
.claude/settings.local.json
.claude/skills/

